<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".uiActivities.NavMenuActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:layout_marginTop="70dp">
    <!-- App Logo -->
    <ImageView
        android:id="@+id/imageViewLogo"
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:layout_marginBottom="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@drawable/logo" />

    <!-- Add Expense -->
    <Button
        android:id="@+id/buttonAddExpense"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/button_background"
        android:textColor="@color/button_text"
        android:text="Add Expense"
        android:padding="10dp"
        android:layout_marginBottom="30dp"/>

    <!-- Set Goal -->
    <Button
        android:id="@+id/buttonSetGoal"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/button_background"
        android:textColor="@color/button_text"
        android:text="Set Goal"
        android:padding="10dp"
        android:layout_marginBottom="30dp"/>

    <!-- View Expenses -->
    <Button
        android:id="@+id/buttonViewExpense"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/button_background"
        android:textColor="@color/button_text"
        android:text="View Expenses"
        android:padding="10dp"
        android:layout_marginBottom="30dp"/>

    <!-- View Totals -per category -->
    <Button
        android:id="@+id/buttonViewTotals"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/button_background"
        android:textColor="@color/button_text"
        android:text="Category Total"
        android:padding="10dp"
        android:layout_marginBottom="30dp"/>

    <!-- graphs -->
    <Button
        android:id="@+id/buttonChartCategory"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/button_background"
        android:textColor="@color/button_text"
        android:text="Category Total View"
        android:padding="10dp"
        android:layout_marginBottom="30dp"/>
</LinearLayout>
</ScrollView>


